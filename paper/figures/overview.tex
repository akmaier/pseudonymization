\begin{figure}[t]
  \centering
  % Laid out in explicit millimetres, which overruns the LNCS text block; scale the finished
  % picture to the column rather than re-deriving every coordinate.
  \resizebox{0.95\textwidth}{!}{%
  \begin{tikzpicture}[
      x=1mm,y=1mm,
      box/.style={draw=blue!55!black, fill=blue!6, rounded corners=1.2mm,
                  line width=.45pt, align=center, inner sep=1.6mm, font=\scriptsize},
      measure/.style={box, draw=orange!70!black, fill=orange!8},
      line/.style={align=left, font=\scriptsize, inner sep=.6mm},
      tag/.style={font=\scriptsize\itshape, text=black!60, inner sep=.6mm},
      hit/.style={fill=teal!16, rounded corners=.6mm, inner sep=.5mm},
      kept/.style={fill=red!12, rounded corners=.6mm, inner sep=.5mm},
      flow/.style={-{Latex[length=1.6mm]}, line width=.5pt, draw=black!70},
      rewrite/.style={-{Latex[length=1.6mm]}, line width=.5pt, draw=teal!60!black, dashed}
    ]
    \node[box, minimum width=24mm, minimum height=16mm] (det) at (0,27)
      {detector pool\\\scriptsize 15 rules, encoders, LLMs\\[-.3mm]
       \scriptsize union / vote / intersection};

    \node[tag] at (30,35) {A};
    \node[line, anchor=west] (a) at (34,35)
      {Frau \tikz[baseline]{\node[hit,anchor=base]{Anna Weber};}, geb.\
       \tikz[baseline]{\node[kept,anchor=base]{12.03.1948};}, wurde in der
       \tikz[baseline]{\node[hit,anchor=base]{Klinik Nord};} vorgestellt.};

    \node[tag] at (30,28) {B};
    \node[line, anchor=west] (b) at (34,28)
      {Frau \tikz[baseline]{\node[hit,anchor=base]{Marta Keller};}, geb.\
       \tikz[baseline]{\node[kept,anchor=base]{12.03.1948};}, wurde in der
       \tikz[baseline]{\node[hit,anchor=base]{Klinik S\"ud};} vorgestellt.};

    \node[tag] at (30,21) {C};
    \node[line, anchor=west] (c) at (34,21)
      {Frau \tikz[baseline]{\node[hit,anchor=base]{\texttt{[PERSON]}};}, geb.\
       \tikz[baseline]{\node[kept,anchor=base]{12.03.1948};}, wurde in der
       \tikz[baseline]{\node[hit,anchor=base]{\texttt{[ORG]}};} vorgestellt.};

    \draw[flow] (det.east) -- (28,35);
    \draw[flow] (det.east) -- (28,28);
    \draw[flow] (det.east) -- (28,21);

    \draw[rewrite] (32,26.4) -- (32,22.6);
    \node[tag, anchor=west, text=teal!50!black] at (24,15.5)
      {identical spans, so B rewrites to C};

    \node[measure, minimum width=23mm] (m1) at (125,35) {detection\\and exposure};
    \node[measure, minimum width=23mm] (m2) at (125,28) {downstream\\utility};
    \node[measure, minimum width=23mm] (m3) at (125,21) {attacks and\\stability};
    \draw[flow] (112,35) -- (m1.west);
    \draw[flow] (112,28) -- (m2.west);
    \draw[flow] (112,21) -- (m3.west);
  \end{tikzpicture}%
  }
  \caption{One set of detector spans generates three release conditions, on an invented sentence in
  the style of a discharge letter. Detected identifiers (teal) become surrogates in B and typed
  placeholders in C; dates (red) pass through by design. B and C replace the same spans, so C is a
  rewrite of B. Every condition meets every measurement family on the same documents.}
  \label{fig:overview}
\end{figure}
