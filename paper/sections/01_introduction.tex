\section{Introduction}\label{sec:introduction}

% 1.25 p. Opens on multimodal sharing: a database is releasable only when both the pixels and the
% prose are anonymised, and AI is what makes either tractable at scale. Packhauser et al. is the
% bridge — A5 is its text analogue. Then the gap (evaluation in halves, aggregates rather than
% per-patient), then the three claims.
